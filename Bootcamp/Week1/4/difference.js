/*
Write a function called difference.
difference should receive two numbers, num1 and num2, as parameters.
difference should return the result of the second number subtracted from the first number.
*/

function difference(num1, num2) {
  return num2 - num1;
}

console.log(difference(3, 5));
